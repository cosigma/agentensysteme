package de.hsb.ants.map;

/**
 * An enum of types which a cell may have from the perspective of an agent.
 * 
 * @author Daniel
 *
 */
public enum CellType {
	START, FREE, BLOCKED, PIT, UNSAFE_UNKNOWN, SAFE_UNKNOWN
}
